<%=
  breadcrumbs(
    { 'Administration' => root_path },
    { 'Available Attributes' => available_attributes_path },
    { @attribute.name => available_attribute_path(@attribute) },
    'Edit'
  )
%>

<%= page_header('Available Attributes', 'Edit Available Attribute') -%>

<%= render(partial: 'form', locals: { submit_label: 'Save' }) %>
