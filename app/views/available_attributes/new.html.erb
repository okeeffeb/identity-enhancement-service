<%=
  breadcrumbs(
    { 'Administration' => root_path },
    { 'Available Attributes' => available_attributes_path },
    'Add'
  )
%>

<%= page_header('Available Attributes', 'New Available Attribute') %>

<%= render(partial: 'form', locals: { submit_label: 'Create' }) %>
